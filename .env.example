# Copy to .env and fill in. NEVER commit the real value.
# A HubSpot private-app token, scopes: automation + marketing email + crm lists.
HUBSPOT_TOKEN=pat-na1-xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# Optional: point at a file that holds the token instead of the value above.
# HUBSPOT_TOKEN_FILE=./.hubspot_token
